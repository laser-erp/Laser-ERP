# См. deploy/env.example — полный список переменных для сервера Ubuntu.
# Локальная разработка: обычно переменные не нужны (используется laser_erp.settings).

DJANGO_SETTINGS_MODULE=laser_erp.settings
# DJANGO_SECRET_KEY=локально-необязательно
